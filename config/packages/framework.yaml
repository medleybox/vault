framework:
    secret: '%env(APP_SECRET)%'
    #csrf_protection: true
    #http_method_override: true

    session:
        enabled: false

    #esi: true
    #fragments: true
    php_errors:
        log: true
